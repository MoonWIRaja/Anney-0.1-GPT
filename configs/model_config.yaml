# Anney 0.1 GPT - Model Configuration
# Upgrade dari ~3M ke ~50M parameter
# Untuk reasoning, emosi, dan personaliti

# === Versi Lama (3M) ===
# vocab_size: 4000
# context_length: 256
# n_embed: 256
# n_heads: 4
# n_layers: 4

# === Versi Baru (50M) ===
vocab_size: 8000
context_length: 512
n_embed: 512
n_heads: 8
n_layers: 14
dropout: 0.1
bias: false
